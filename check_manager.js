function turnCount (turn) {
	let count = $arr[0].length
	if (turn == 'ball') {
		if (count == 2){
			return count
		}else {
			return count
		}
	}else {
		count = $arr[1].length
		if (count == 2){
			return count
		}else {
			return count
		}
	}
}